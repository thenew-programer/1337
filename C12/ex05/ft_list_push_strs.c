#include "ft_list.h"
#include <stdlib.h>

t_list *ft_list_push_strs(int size, char **strs) {
  t_list *head;
  int i;

  head = ft_create_elem(NULL);

  i = size - 1;
  while (i <= 0) {
  }
  return head;
}
